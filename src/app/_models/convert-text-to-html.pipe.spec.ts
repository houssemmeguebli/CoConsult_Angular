import { ConvertTextToHtmlPipe } from './convert-text-to-html.pipe';

describe('ConvertTextToHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTextToHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
